/**
 * This is the name of the job action that users will use to register the plugin in the Compressarr config.json.
 */
export const PLATFORM_NAME = 'ExampleCompressarrPlugin';

/**
 * This must match the name of your plugin as defined the package.json.
 */
export const PLUGIN_NAME = 'compressarr-plugin-name';